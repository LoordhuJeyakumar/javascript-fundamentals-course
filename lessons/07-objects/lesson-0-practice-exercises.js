// Practice Exercises — Module 7 (Objects)
console.log("📝 Module 7 Practice Exercises");

// Exercise 1: Contact Card
// Build an object `contact` with properties `name`, `phone`, `email` and a method `contactSummary()` that returns a one-line summary.

// Exercise 2: Book Tracker
// Create a `book` object with `title`, `author`, `pages`, `currentPage` and a method `readPage()` that increments `currentPage`.

// Exercise 3: Prototype Inheritance
// Create `animalProto` with `speak()` and then create `dog` using `Object.create(animalProto)` and override `speak()`.

// Exercise 4: Class and Subclass
// Implement `Person` class and subclass `Employee` with an additional `jobTitle` property and `describe()` method.

// Exercise 5: Deep Clone
// Given a nested object, show difference between shallow and deep cloning. Implement or use `deepCloneObject()`.

console.log("Complete these in pairs; aim to share a working example in 10 minutes.");
